# SEO改善 未着手タスク一覧

起票日: 2026-04-13  
出典: `_draft/サイト構成SEO・運営ウォークポイントレポート_2026-04-13.md`

---

## 優先度: 高

### [ ] T01 — テンプレートページをナビ・内部リンクから除去

**目的**: noindex 済みのページへのリンクを残すとクロール予算を消費する。完全に到達不能にする。  
**作業場所**: `src/navigation.ts`（フッター・ヘッダーのリンク定義）  
**手順**:

1. `src/navigation.ts` を開き、`/services`, `/pricing`, `/homes/*`, `/landing/*` へのリンクを確認・削除
2. フッターウィジェット等で直書きされているリンクがあれば同様に削除
3. `pnpm build` でビルドエラーがないことを確認

---

### [ ] T02 — Contactフォームの実機表示確認（PC / スマホ）

**目的**: iframe URL 修正後、実際にフォームが表示・送信できるかを確認する。  
**作業場所**: `src/pages/contact.astro`  
**手順**:

1. `pnpm dev` で開発サーバーを起動
2. PC ブラウザで `/contact` を開き、Google Forms が正常表示されることを確認
3. スマホ（またはDevTools のモバイルエミュレーション）で同様に確認
4. テスト送信を行い、受信側（sasisi344@gmail.com）にメールが届くことを確認

---

## 優先度: 中

### [ ] T03 — Contact 送信完了の GA4 イベント計測追加

**目的**: 問い合わせ数をGA4で計測し、CVR改善の判断材料にする。  
**作業場所**: `src/pages/contact.astro`  
**手順**:

1. Google Forms の送信完了を検知する方法を選択（iframeの`onload`イベント or URL変化の監視）
2. `gtag('event', 'form_submit', { event_category: 'contact' })` を発火させるスクリプトを追加
3. GA4のリアルタイムレポートでイベントが計測されることを確認

---

### [ ] T04 — 関連記事ロジックを「同一試験」「同一難易度」軸で強化

**目的**: 記事消費後の離脱を防ぎ、同一試験の記事へ誘導して回遊率を高める。  
**作業場所**: `src/components/blog/RelatedPosts.astro`  
**手順**:

1. 現状の関連記事選定ロジックを確認
2. `tags` 配列に同一の試験名タグ（例: `基本情報技術者`）を持つ記事を優先スコアリングする実装を追加
3. 試験名タグ一致 > カテゴリ一致 > その他 の優先順位で表示順を制御
4. 表示件数・UIに変更がないことをビルドで確認

---

## 優先度: 低〜中期

### [ ] T05 — カテゴリTOPに「学習ロードマップ」固定セクション追加

**目的**: FE/ITパスポート等の試験別に、記事を学習順序で並べた導線を設置して初訪問ユーザーの離脱を防ぐ。  
**作業場所**: `src/pages/[...blog]/[category]/` 付近のカテゴリページ  
**手順**:

1. カテゴリページのテンプレートを確認
2. `method` カテゴリを優先対象として、試験別ロードマップセクション（固定の記事リスト）コンポーネントを設計
3. コンテンツ（どの記事をどの順番で並べるか）を定義してハードコードまたはデータファイル化
4. モバイル表示・ダークモードでの崩れがないことを確認

---

### [ ] T06 — 重要カテゴリにピラーページを設置

**目的**: 「ITパスポート完全攻略ガイド」などのハブ記事を設置してトピック権威性を高める。  
**作業場所**: `src/data/post/{category}/` （新規記事作成）  
**手順**:

1. ピラーページ対象の試験・テーマを選定（推奨: ITパスポート / 基本情報技術者 を優先）
2. `.agents/post_writer.md` の QUEST フレームワークで構成を設計
3. 既存記事へのリンクを多数含む「まとめ・ハブ」形式で執筆
4. カテゴリTOPおよび関連記事からリンクを張る

---

## 参考：完了済みタスク（2026-04-13実施）

| タスク                          | 実施内容                                                                                   |
| ------------------------------- | ------------------------------------------------------------------------------------------ |
| ✅ テンプレートページ noindex化 | services / pricing / homes/_ / landing/_ に `robots: { index: false, follow: false }` 追加 |
| ✅ 主要ページUI日本語統一       | about / contact / ブログ一覧 / 404 を日本語化                                              |
| ✅ robots.txt 整備              | /decapcms/, /homes/, /landing/ 等を Disallow に追加                                        |
| ✅ ContactフォームURL修正       | `?embedded=true` 形式に修正                                                                |
| ✅ JSON-LD実装                  | WebSite / Organization / BlogPosting / BreadcrumbList を追加                               |
| ✅ sitemap lastmod反映          | frontmatter の `lastmod` を sitemap に反映する serialize 関数を実装                        |
